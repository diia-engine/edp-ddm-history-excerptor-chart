apiVersion: v1
kind: Service
metadata:
  name: {{ include "history-excerptor-chart.fullname" . }}
  labels:
    {{- include "history-excerptor-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "history-excerptor-chart.selectorLabels" . | nindent 4 }}
