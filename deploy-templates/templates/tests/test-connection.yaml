apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "history-excerptor-chart.fullname" . }}-test-connection"
  labels:
    {{- include "history-excerptor-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "history-excerptor-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
